import * as types from '../constants/ActionTypes';

function blacklistReducer(state = { sites: [], blacklistTextEntry: ""}, action){
  switch(action.type){
    default:
        return state;
  }
}

export default blacklistReducer;
